export enum ContactMethodType {
    EMAIL = 'email',
    PHONE = 'phone',
    PUSH = 'push',
    WEBHOOK = 'webhook',
    WHATSAPP = 'whatsapp',
    TELEGRAM = 'telegram',
    SLACK = 'slack',
    DISCORD = 'discord'
}

export default ContactMethodType;
