import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.infolink.charts.app',
  appName: 'nuxt-app',
  webDir: '.output/public'
};

export default config;
